passdb {
  driver = passwd-file
  args = scheme=SHA512-CRYPT username_format=%n /etc/exim/domains/%d/passwd
}

userdb {
  driver = passwd-file
  args = username_format=%n /etc/exim/domains/%d/passwd
    override_fields = uid=dovecot gid=dovecot
}
